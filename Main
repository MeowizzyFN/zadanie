#include <iostream>
#include <string>
#include <string.h>
#include <fstream>
#include <locale>


using namespace std;

int main(int argc, char* argv[])
{
	setlocale(LC_ALL, "RUSSIAN");

	for (int i = 0; argv[i]; i++) {
		if (strcmp(argv[i], "/?") == 0) {
			cout << "Its your help" << endl;
		}
		for (int i = 0; argv[i]; i++) {
			if (strcmp(argv[i], "read and change") == 0) {

			}
		}
		return 0;
	}
}

 
